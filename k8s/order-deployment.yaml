apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: lux-gaming
  labels:
    app: order-service
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: rajithawan/lugx-order-image:latest
        ports:
        - containerPort: 3004
        env:
        - name: PORT
          value: "3004"
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: lux-gaming-config
              key: NODE_ENV
        - name: HOST
          valueFrom:
            configMapKeyRef:
              name: lux-gaming-config
              key: HOST
        - name: DATABASE_URL_DEV
          valueFrom:
            secretKeyRef:
              name: lux-gaming-secrets
              key: DATABASE_URL_DEV
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: lux-gaming-secrets
              key: JWT_SECRET
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: lux-gaming-config
              key: LOG_LEVEL
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 5
          periodSeconds: 5