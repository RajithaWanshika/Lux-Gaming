apiVersion: v1
kind: Service
metadata:
  name: game-service
  namespace: lux-gaming
  labels:
    app: game-service
spec:
  selector:
    app: game-service
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
  type: ClusterIP