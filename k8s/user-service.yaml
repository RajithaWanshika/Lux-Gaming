apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: lux-gaming
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
  - port: 80
    targetPort: 3002
    protocol: TCP
  type: ClusterIP