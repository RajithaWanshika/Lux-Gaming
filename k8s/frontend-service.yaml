apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: lux-gaming
  labels:
    app: frontend-service
spec:
  selector:
    app: frontend-service
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP 